/**
 * entiendo que se puede declarar el archivo como interface. porque a nivel de compilacion las interfaces no guardan espacio en memoria.
 */
export interface AddCategoryRequest {
  Name: string;
  UrlHandle: string;
}
